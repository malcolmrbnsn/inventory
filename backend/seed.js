// seed.js

const db = require("./models")

// const admin = new db.User({email: "assd", password: "asdfsad"})
// admin.save()
// .then(() => console.log('saved'))
// .catch(e => console.log(e))

// db.User.find().then(users => console.log(users))